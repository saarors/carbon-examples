import Core library "io";
import Core library "range";

// TODO inline functions
// inline fn Mult(a : i32, b : i32) -> i32 {
// 	return a * b;
// }

// Main function
fn Run() -> i32 {
	var x : i32 = 2;
	var y : i32 = 3;
	// var z : i32 = Mult(x, y);
	// Core.Print(z);

	return 0;
}
